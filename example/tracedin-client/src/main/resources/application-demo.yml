spring:
  kafka:
    producer:
      bootstrap-servers:
       - ${KAFKA_ENDPOINT}

traced-in:
  service-name: ${TRACEDIN_SERVICE_NAME}
  project-key: ${TRACEDIN_PROJECT_KEY}
  host: ${TRACEDIN_HOST}
  demo:
    service:
      url: ${TRACEDIN_SERVICE_ENDPOINT}
